module.exports = {
    name: 'skip',
    description: "Skip the current music, if there is nbothing else in the queue disconnect the bot from the vocal channel",
    execute(message, args){
        // nothing here
    }
}